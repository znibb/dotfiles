# Use best available audio quality
-f bestaudio

# Extract audio to preferred format
-x
--audio-format mp3
--audio-quality 0 # 0 = best

# Output filename template
-o "%(title)s.%(ext)s"

# Add metadata and thumbnail to file
--embed-metadata
--embed-thumbnail
--convert-thumbnails jpg